---
name: "1-D-1: CMakeLists.txt with build presets"
status: closed
created: 2026-04-24T18:42:05Z
updated: 2026-04-24T18:50:33Z
github: https://github.com/Sri-Harsha-T/qfault_cc/issues/2
depends_on: []
parallel: false
conflicts_with: []
---

# Task: 1-D-1 — CMakeLists.txt with build presets

## Description

Write the root `CMakeLists.txt` and `CMakePresets.json` for the QFault project. This is the first task and unblocks all subsequent code work. No source files exist yet — this establishes the build skeleton.

## Acceptance Criteria

- [ ] `CMakeLists.txt` targets C++20, minimum CMake 3.16
- [ ] `CMakePresets.json` defines four presets: `debug`, `release`, `asan`, `coverage`
- [ ] GoogleTest 1.14+ fetched via `FetchContent` — no system dependency required
- [ ] Google Benchmark 1.8+ fetched via `FetchContent`, gated by `-DQFAULT_RUN_BENCHMARKS=ON`
- [ ] Library target `libqfault` defined (empty sources list for now — sources added per story)
- [ ] Test binary target `qfault_tests` links GoogleTest and `libqfault`
- [ ] `cmake --preset debug && cmake --build build/debug` succeeds (even with no source files yet)
- [ ] `ctest --test-dir build/debug` runs (no tests yet, but exits 0)
- [ ] CMake flags documented in `memory-bank/techContext.md`:
  - `-DQFAULT_ENABLE_ASAN=ON`
  - `-DQFAULT_ENABLE_UBSAN=ON`
  - `-DQFAULT_RUN_BENCHMARKS=ON`
  - `-DQFAULT_ENABLE_PYTHON=ON`
  - `-DQFAULT_GRIDSYNTH_PATH=<path>`

## Technical Details

- Source root: `src/qfault/` — use `target_include_directories` with `include/`
- Both clang-17+ and gcc-13+ must build without warnings (`-Wall -Wextra -Wpedantic -Werror`)
- ASAN preset adds: `-fsanitize=address,undefined -fno-omit-frame-pointer`
- Coverage preset adds: `--coverage -fprofile-arcs -ftest-coverage`
- `build/` must be in `.gitignore`

## Dependencies

None — this is the root task.

## Effort Estimate
- Size: M
- Hours: 4
